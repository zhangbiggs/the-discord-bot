# ── Runtime ─────────────────────────────────────────────────────────────
fastapi>=0.115.0              # Web framework (provides FastAPI, HTTPException, Request)
uvicorn[standard]>=0.34.0     # ASGI server
httpx>=0.28.0                 # Async HTTP client
pydantic>=2.0.0               # Data validation (BaseModel, Field) — bundled with fastapi

# ── Dev / Tests ─────────────────────────────────────────────────────────
pytest>=8.0.0                 # Test framework
pytest-asyncio>=0.24.0        # Async test support (asyncio_mode = auto)
